def multiplicar(x, y):
    return x * y

print(multiplicar(3, 5))

##Creamos la funcion m_lambda que recibe los argumentos x e y. Devuelve la multiplicación de ambos
m_lambda = lambda x, y: x * y

print(m_lambda(6,4))